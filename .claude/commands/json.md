---
description: Convertit un prompt en JSON structuré, sans exécuter la demande
argument-hint: <ton prompt>
---

Analyse le prompt ci-dessous en suivant exactement les règles et le schéma de
`.claude/agents/prompt-to-json.md`. Renvoie uniquement l'objet JSON, dans un bloc
```json, et n'exécute pas la demande.

Prompt :

$ARGUMENTS
